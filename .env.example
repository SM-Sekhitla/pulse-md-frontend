FRONTEND_PORT=8080

# Public API path used by the browser bundle. Keep this relative when nginx proxies /api.
VITE_API_URL=/api/v1

# Backend origin used by nginx inside the frontend container.
# For local separate compose stacks on Linux, use http://host.docker.internal:5000.
# In production, set this to your backend service URL, for example https://api.example.com.
VITE_API_TARGET=http://host.docker.internal:5000

# Public frontend origin used in generated QR verification links.
VITE_FRONTEND_BASE_URL=http://localhost:8080
VITE_GIT_VERSION=dev

# Only needed for Vite dev server, not nginx production serving.
VITE_ALLOWED_HOSTS=localhost
